fn main() {
    let numbers = vec![1, 2, 3, 4, 5];
    let a = 2;
    let b = 3;

    // print the location and contents of the variable
    dbg!(numbers);

    // when expression is used, the expression + result will be printed
    dbg!(a + b);
}
